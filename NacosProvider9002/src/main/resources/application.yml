server:
  port: 9002


spring:
  application:
    name: nacos-provider
  cloud:
    discovery:
      server-addr: 192.168.0.101:80

management:
  endpoint:
    web:
      exposure:
        include: '*'